const { BasePage } = require('./basePage');
const { MainPage } = require('./mainPage');
const { LoginPage } = require('./loginPage');
const { HomePage } = require('./homePage');

module.exports = { BasePage, MainPage, LoginPage, HomePage }; 